{
  "name": "emotion-detection-api",
  "version": "1.0.0",
  "description": "TFLite Emotion Detection API for Flutter App",
  "main": "server.js",
  "scripts": {
    "start": "pip install -r requirements.txt && node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^2.0.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
